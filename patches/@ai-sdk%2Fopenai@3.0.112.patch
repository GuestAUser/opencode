diff --git a/dist/index.js b/dist/index.js
index fd1d715af0d3b8354b56c36c6f677fd6faa3d8f8..e26bf55e7478a8719cfb54ecb95d615aa9e83ba7 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1085,22 +1085,6 @@ var OpenAIChatLanguageModel = class {
         });
       }
     }
-    if (openaiOptions.serviceTier === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      baseArgs.service_tier = void 0;
-    }
-    if ((openaiOptions.serviceTier === "priority" || openaiOptions.serviceTier === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      baseArgs.service_tier = void 0;
-    }
     const {
       tools: openaiTools2,
       toolChoice: openaiToolChoice,
@@ -6082,22 +6066,6 @@ var OpenAIResponsesLanguageModel = class {
         });
       }
     }
-    if ((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      delete baseArgs.service_tier;
-    }
-    if (((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "priority" || (openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      delete baseArgs.service_tier;
-    }
     const shellToolEnvType = (_m = (_l = (_k = tools == null ? void 0 : tools.find(
       (tool) => tool.type === "provider" && tool.id === "openai.shell"
     )) == null ? void 0 : _k.args) == null ? void 0 : _l.environment) == null ? void 0 : _m.type;
diff --git a/dist/index.mjs b/dist/index.mjs
index bf0dfd74b494fbce8de6b001abc901a3ef15d6e4..46b8dabd41ddea7a95776edc1482397a43dde22d 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1081,22 +1081,6 @@ var OpenAIChatLanguageModel = class {
         });
       }
     }
-    if (openaiOptions.serviceTier === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      baseArgs.service_tier = void 0;
-    }
-    if ((openaiOptions.serviceTier === "priority" || openaiOptions.serviceTier === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      baseArgs.service_tier = void 0;
-    }
     const {
       tools: openaiTools2,
       toolChoice: openaiToolChoice,
@@ -6188,22 +6172,6 @@ var OpenAIResponsesLanguageModel = class {
         });
       }
     }
-    if ((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      delete baseArgs.service_tier;
-    }
-    if (((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "priority" || (openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      delete baseArgs.service_tier;
-    }
     const shellToolEnvType = (_m = (_l = (_k = tools == null ? void 0 : tools.find(
       (tool) => tool.type === "provider" && tool.id === "openai.shell"
     )) == null ? void 0 : _k.args) == null ? void 0 : _l.environment) == null ? void 0 : _m.type;
diff --git a/dist/internal/index.js b/dist/internal/index.js
index c2faa4d32b6a5731d9bfc46877093e26cf8c8140..35ae63576246fabe9730822e3877a33ad90e29f0 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -1120,22 +1120,6 @@ var OpenAIChatLanguageModel = class {
         });
       }
     }
-    if (openaiOptions.serviceTier === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      baseArgs.service_tier = void 0;
-    }
-    if ((openaiOptions.serviceTier === "priority" || openaiOptions.serviceTier === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      baseArgs.service_tier = void 0;
-    }
     const {
       tools: openaiTools,
       toolChoice: openaiToolChoice,
@@ -6349,22 +6333,6 @@ var OpenAIResponsesLanguageModel = class {
         });
       }
     }
-    if ((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      delete baseArgs.service_tier;
-    }
-    if (((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "priority" || (openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      delete baseArgs.service_tier;
-    }
     const shellToolEnvType = (_m = (_l = (_k = tools == null ? void 0 : tools.find(
       (tool) => tool.type === "provider" && tool.id === "openai.shell"
     )) == null ? void 0 : _k.args) == null ? void 0 : _l.environment) == null ? void 0 : _m.type;
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 785d7688f7ab3ea5eb7b19ef77f77f71b349f3cd..015b03b7f14f8d3884e94f4d34249b2d7a392cde 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -1073,22 +1073,6 @@ var OpenAIChatLanguageModel = class {
         });
       }
     }
-    if (openaiOptions.serviceTier === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      baseArgs.service_tier = void 0;
-    }
-    if ((openaiOptions.serviceTier === "priority" || openaiOptions.serviceTier === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      baseArgs.service_tier = void 0;
-    }
     const {
       tools: openaiTools,
       toolChoice: openaiToolChoice,
@@ -6430,22 +6414,6 @@ var OpenAIResponsesLanguageModel = class {
         });
       }
     }
-    if ((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "flex" && !modelCapabilities.supportsFlexProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "flex processing is only available for o3, o4-mini, and gpt-5 models"
-      });
-      delete baseArgs.service_tier;
-    }
-    if (((openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "priority" || (openaiOptions == null ? void 0 : openaiOptions.serviceTier) === "fast") && !modelCapabilities.supportsPriorityProcessing) {
-      warnings.push({
-        type: "unsupported",
-        feature: "serviceTier",
-        details: "priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported"
-      });
-      delete baseArgs.service_tier;
-    }
     const shellToolEnvType = (_m = (_l = (_k = tools == null ? void 0 : tools.find(
       (tool) => tool.type === "provider" && tool.id === "openai.shell"
     )) == null ? void 0 : _k.args) == null ? void 0 : _l.environment) == null ? void 0 : _m.type;
diff --git a/src/chat/openai-chat-language-model.ts b/src/chat/openai-chat-language-model.ts
index 32fa12b0099149bdd802255e2f1a28c4c2d9d1d9..758dd9e317cb9f5d608b5585411ba37f50266e37 100644
--- a/src/chat/openai-chat-language-model.ts
+++ b/src/chat/openai-chat-language-model.ts
@@ -297,35 +297,6 @@ export class OpenAIChatLanguageModel implements LanguageModelV3 {
       }
     }
 
-    // Validate flex processing support
-    if (
-      openaiOptions.serviceTier === 'flex' &&
-      !modelCapabilities.supportsFlexProcessing
-    ) {
-      warnings.push({
-        type: 'unsupported',
-        feature: 'serviceTier',
-        details:
-          'flex processing is only available for o3, o4-mini, and gpt-5 models',
-      });
-      baseArgs.service_tier = undefined;
-    }
-
-    // Validate priority processing support
-    if (
-      (openaiOptions.serviceTier === 'priority' ||
-        openaiOptions.serviceTier === 'fast') &&
-      !modelCapabilities.supportsPriorityProcessing
-    ) {
-      warnings.push({
-        type: 'unsupported',
-        feature: 'serviceTier',
-        details:
-          'priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported',
-      });
-      baseArgs.service_tier = undefined;
-    }
-
     const {
       tools: openaiTools,
       toolChoice: openaiToolChoice,
diff --git a/src/responses/openai-responses-language-model.ts b/src/responses/openai-responses-language-model.ts
index 1001f5ffec51ef4dbcbc38acdd14e682c7753ed4..cef0f67c9c00458c4287302e2637f288d856446c 100644
--- a/src/responses/openai-responses-language-model.ts
+++ b/src/responses/openai-responses-language-model.ts
@@ -520,37 +520,6 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
       }
     }
 
-    // Validate flex processing support
-    if (
-      openaiOptions?.serviceTier === 'flex' &&
-      !modelCapabilities.supportsFlexProcessing
-    ) {
-      warnings.push({
-        type: 'unsupported',
-        feature: 'serviceTier',
-        details:
-          'flex processing is only available for o3, o4-mini, and gpt-5 models',
-      });
-      // Remove from args if not supported
-      delete (baseArgs as any).service_tier;
-    }
-
-    // Validate priority processing support
-    if (
-      (openaiOptions?.serviceTier === 'priority' ||
-        openaiOptions?.serviceTier === 'fast') &&
-      !modelCapabilities.supportsPriorityProcessing
-    ) {
-      warnings.push({
-        type: 'unsupported',
-        feature: 'serviceTier',
-        details:
-          'priority processing is only available for supported models (gpt-4, gpt-5, gpt-5-mini, o3, o4-mini) and requires Enterprise access. gpt-5-nano is not supported',
-      });
-      // Remove from args if not supported
-      delete (baseArgs as any).service_tier;
-    }
-
     const shellToolEnvType = (
       tools?.find(
         tool => tool.type === 'provider' && tool.id === 'openai.shell',
